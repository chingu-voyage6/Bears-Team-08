// import { Store, Middleware, MiddlewareAPI, Dispatch, Action } from "redux";
// import { State } from "../reducers";

// export const token: Middleware = <S, D>(api: MiddlewareAPI<D, S>) => (
//   next: Dispatch<void>
// ) => <A extends Action>(action: Action) => {
// 	const state = api.getState()
// 	api.dispatch()
//   return next(action);
// };

// export const token = api => next => actiont => {};
