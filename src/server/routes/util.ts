// import { Request, Response, NextFunction } from "express";

// export const findObject = kind => (
//   req: Request,
//   res: Response,
//   next: NextFunction,
//   value: any
// ) => {
//   const kindPlural = `${kind}s`;
//   const obj = {};

//   if (obj) {
//     req[kind] = obj;
//     next();
//   } else {
//     res.status(404).json;
//   }
// };
