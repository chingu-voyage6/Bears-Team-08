// import { Router } from "express";

// const single = (req, res) => {};

// const all = (req, res) => {};

// const getRouter = (): Router => {
//   const router = Router();
//   router.param("userId", () => {});

//   router.use("/", all);
//   router.use("/:userId", single);
//   return router;
// };

// export const router: Router = getRouter();
